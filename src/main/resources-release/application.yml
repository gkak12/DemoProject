spring:
  datasource:
    url: jdbc:postgresql://111.111.111.111:555/postgres
    username: postgres_release
    password: postgres_release
    hikari:      connection-timeout: 3000
      minimum-idle: 1
      maximum-pool-size: 4
  jpa:    database: postgresql
    
api:
  url: https://httpbin.org/get